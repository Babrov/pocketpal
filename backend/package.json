{
  "name": "@pocketpal/backend",
  "author": "Illia Babrov",
  "license": "ISC",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm nest build",
    "clear": "pnpm rimraf dist && pnpm rimraf node_modules/.cache",
    "clear:deps": "pnpm rimraf node_modules && pnpm clear",
    "lint": "eslint --quiet --fix src",
    "start": "pnpm nest start",
    "start:debug": "pnpm nest --debug --watch",
    "start:dev": "pnpm nest start --watch",
    "start:prod": "node dist/main"
  },
  "dependencies": {
    "@nestjs/common": "^10.2.7",
    "@nestjs/core": "^10.2.7",
    "@nestjs/config": "^3.1.1",
    "@nestjs/platform-express": "^10.2.8",
    "@nestjs/typeorm": "^10.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.0",
    "rimraf": "^5.0.5",
    "typeorm": "^0.3.17",
    "pg": "^8.11.3",
    "@pocketpal/contracts": "workspace:*"
  }
}
